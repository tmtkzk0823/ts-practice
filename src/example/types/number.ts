// 方注釈
const count: number = 123;

// NaN;
// Not a Numberを表す変数。JSでは処理結果が数値にならない場合はNaNを返すことがある。
// 文字列を数値に変換するperseInt関数は、数値にできない場合はNaNを返す
const price = parseInt("百円");
console.log(price);
// → NaNを返す
